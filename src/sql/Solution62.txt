SELECT c.HISTORY_ID, c.CAR_ID, DATE_FORMAT(c.START_DATE, '%Y-%m-%d') AS START_DATE, DATE_FORMAT(c.END_DATE, '%Y-%m-%d') AS END_DATE, IF(DATEDIFF(END_DATE, START_DATE) +1 >=30,'장기 대여', '단기 대여') AS RENT_TYPE
FROM CAR_RENTAL_COMPANY_RENTAL_HISTORY c
WHERE c.START_DATE LIKE '2022-09%'
ORDER BY 1 DESC