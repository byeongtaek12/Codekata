SELECT a.ANIMAL_ID, a.ANIMAL_TYPE, a.NAME
FROM ANIMAL_INS a JOIN ANIMAL_OUTS b ON a.ANIMAL_ID=b.ANIMAL_ID
WHERE (b.SEX_UPON_OUTCOME LIKE 'Spayed%' OR b.SEX_UPON_OUTCOME LIKE'Neutered%') AND a.SEX_UPON_INTAKE LIKE 'Intact%'
GROUP BY 1