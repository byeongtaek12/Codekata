SELECT g.SCORE, HE.EMP_NO, HE.EMP_NAME, HE.POSITION, HE.EMAIL
FROM (
    SELECT SUM(HG.SCORE) AS SCORE, HG.EMP_NO
    FROM HR_GRADE HG
    GROUP BY 2
    ORDER BY 1 DESC
    LIMIT 1
) g
JOIN HR_EMPLOYEES HE ON g.EMP_NO = HE.EMP_NO