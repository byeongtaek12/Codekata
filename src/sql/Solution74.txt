SELECT s.ROUTE, CONCAT(ROUND(SUM(s.D_BETWEEN_DIST),1),'km') AS TOTAL_DISTANCE,
CONCAT(ROUND(AVG(s.D_BETWEEN_DIST),2),'km') AS AVERAGE_DISTANCE
FROM SUBWAY_DISTANCE s
GROUP BY 1
ORDER BY SUM(s.D_BETWEEN_DIST) DESC
